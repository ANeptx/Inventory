import React from 'react'
import { Button } from '../ui/button'

export const NavbarItem = () => {
    return (
        <div>
            <Button className='bg-red-700'>Logout</Button>
        </div>

    )
}
